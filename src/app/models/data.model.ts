//this model describe how the data is organize and which kind of value there are inside the json file

export class DataModel {
        id: string;
        name: string;
        type: string;
        niveau: string;
        longitude: number
        latitude: number
};

